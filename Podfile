# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'WconnEct' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks

pod 'Google/SignIn'
  use_frameworks!

  # Pods for WconnEct

  target 'WconnEctTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'WconnEctUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
